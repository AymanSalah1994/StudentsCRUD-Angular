export class Student {
  // This is the Model Class
  constructor(public id:number , public name:string , public age:number)
  // constructor(public id:number , public name?:string , public age:number=99)
  // If we Use Question Mark , it means this Field is optional like name
  // We Can also Make Default Values if we Want So Like age
  {
    // NOTHING more is Needed , We Can Now just Use the CTOR to Create Objects
  }
}
